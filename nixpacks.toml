[phases.setup]
nixPkgs = ["python311" "ffmpeg" "glibcLocales"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "gunicorn web_app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 4 --threads 2"